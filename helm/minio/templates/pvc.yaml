apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
  namespace: devsecops-prod
  labels:
    app: {{ .Release.Name }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size | default "5Gi" }}