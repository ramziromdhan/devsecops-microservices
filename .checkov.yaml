skip-check:
  - CKV_K8S_21   # default namespace — namespace dédié géré séparément
  - CKV_K8S_40   # UID > 10000 — UID 1001 intentionnel
  - CKV_K8S_15   # pullPolicy Always — Never intentionnel pour k3s local
  - CKV_K8S_14   # image tag — SHA commit utilisé
  - CKV_K8S_43   # image digest — SHA commit suffit
  - CKV_K8S_35   # secrets as files — Kubernetes Secrets utilisés
  # Gateway (Nginx) — nécessite NET_BIND_SERVICE pour port 80
  - CKV_K8S_28   # NET_RAW — non applicable car on drop ALL et add NET_BIND_SERVICE
  # PostgreSQL — tourne avec son propre utilisateur système
  - CKV_K8S_23   # runAsNonRoot — PostgreSQL doit tourner en root
  - CKV_K8S_30   # securityContext container — appliqué partiellement sur DB
  - CKV_K8S_20   # allowPrivilegeEscalation — PostgreSQL en a besoin
  - CKV_K8S_22   # readOnlyRootFilesystem — PostgreSQL écrit dans son datadir
  - CKV_K8S_37   # capabilities — PostgreSQL nécessite ses capabilities
  - CKV_K8S_31   # seccomp — appliqué au niveau pod, pas container
  - CKV_K8S_25   # capabilities added — NET_BIND_SERVICE pour Nginx gateway
