apiVersion: v1
kind: Service
metadata:
  name: file-service-go
  namespace: devsecops-prod
  labels:
    app: file-service-go
    monitored: "true"
spec:
  selector:
    app: file-service-go
  ports:
    - name: http
      port: 8003
      targetPort: 8003
  type: ClusterIP
